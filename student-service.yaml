server:
  port: 8081
  servlet:
    context-path: /api/v1/students
  # forward-headers-strategy: framework

spring:
  profiles:
      active: monitoring
  datasource:
    username: dhaval
    password: dhaval123
    url: jdbc:postgresql://localhost:5433/userdb

springdoc:
  packages-to-scan: com.lms.student_service.controller
  paths-to-match: /**
 
