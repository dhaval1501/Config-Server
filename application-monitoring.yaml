management:
  endpoints:
    web:
      exposure:
        include: *

  endpoint:
    health:
      show-details: always

  # prometheus:
  #   metrics:
  #     export:
  #       enabled: true

  tracing:
    sampling:
      probability: 1.0

  # zipkin:
  #   tracing:
  #     endpoint: http://localhost:9411/api/v2/spans
