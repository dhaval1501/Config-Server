server:
  port: 8083
  servlet:
    context-path: /api/v1/loans

spring:
  datasource:
    username: dhaval
    password: dhaval123
    url: jdbc:postgresql://localhost:5433/loandb
  kafka:
    bootstrap-servers: localhost:9092

springdoc:
  packages-to-scan: com.lms.loan_service.controller
  paths-to-match: /**
